# Метод	str.endswith(suffix)
# Описание	Возвращает True, если строка оканчивается на подстроку suffix. 
# Иначе возвращает False. suffix может быть кортежем проверяемых окончаний строки
s = "Hello, world!"
end = s.endswith("world!")
print(end)
# Результат True
